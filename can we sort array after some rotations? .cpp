if there is an array and you want to know that after some left or right rotation array can be sorted or not so 

if(arr[i]>arr[i+1]) drops++;



if(drops==1) so array can be sorted after rotations

if(drops==0) array is already sorted

if(drops>1) array can not be sorted after rotations
